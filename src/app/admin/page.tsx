import { AdminPanel } from '@/components/admin/AdminPanel/AdminPanel';

export const metadata = {
  title: 'Admin | Smart Email Manager',
  description: 'Email Manager administration',
};

export default function AdminPage() {
  return <AdminPanel />;
}