import { AuthForm } from '@/components/auth/AuthForm';

export const metadata = {
  title: 'Sign In | Smart Email Manager',
  description: 'Sign in to your account',
};

export default function AuthPage() {
  return <AuthForm />;
}