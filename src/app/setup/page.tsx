import { SetupGuide } from '@/components/setup/SetupGuide';

export const metadata = {
  title: 'Setup | Smart Email Manager',
  description: 'Set up your Smart Email Manager system',
};

export default function SetupPage() {
  return <SetupGuide />;
}